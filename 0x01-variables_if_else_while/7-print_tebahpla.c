#include <stdio.h>
/**
 * main - Entry point
 * Description: 'print alphabets in reverse'
 * Return: Always 0 (Success)
 */
int main(void)
{
	int c;

	c = 0;
	while
		(c >= 97) {
			putchar(c + '0');
			c++;
		}
	putchar('\n');
	return (0);
}
